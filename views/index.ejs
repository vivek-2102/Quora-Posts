<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css"/>
</head>
<body>
    <h1>Quora Posts</h1>
    <% for( post of posts) { %>
        <div>
        <h2>@<%= post.username %></h2>
        <p><%= post.content %></p>
        <a href="http://localhost:8080/posts/<%= post.id %>">See your detailed post</a>
        <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
        <br>
        <form method="post" action="/posts/<%=post.id%>?_method=DELETE"><button>Delete</button></form>
         
        <% } %>
         
</div>
        <a href="http://localhost:8080/posts/new">Add a post</a>
</body>
</html>